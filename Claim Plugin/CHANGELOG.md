# Changelog

## Unreleased
- Added shared API stubs (RankProvider/TeamProvider/JobProvider/RankView) to prepare unified player display and cross-plugin data sharing.
- Documented planned display ownership in integration report.
- Added `/ranks` Chat-Command für Rank-Verwaltung (Erstellen, Löschen, Flags, Properties, Reload) inkl. deutscher Fehlermeldungen.
- Tab-Branding (Header/Footer) wird beim Join und nach Reload gesetzt und ist konfigurierbar.
- Mausrad-Autosortierung für Inventare & Container mit Optionen in `config.yml` und Actionbar-Rückmeldung.
- Nachrichten ausgelagert in `messages.yml` mit neuen Texten für Rank-Befehle und Autosort.
