plugins {
    id 'java'
}

group = 'com.zbennoz'
version = '0.1.0'

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(21)
    withSourcesJar()
}

repositories {
    mavenCentral()
    maven { url = 'https://repo.papermc.io/repository/maven-public/' }
    maven { url = 'https://jitpack.io' }
}

dependencies {
    compileOnly 'io.papermc.paper:paper-api:1.21-R0.1-SNAPSHOT'
    compileOnly 'com.github.MilkBowl:VaultAPI:1.7'

    implementation 'org.xerial:sqlite-jdbc:3.46.0.0'
}

// Do not ship plugin resources in the sources jar so it cannot be mistaken for a plugin on the server.
tasks.named('sourcesJar') {
    exclude('plugin.yml', 'config.yml', 'messages.yml')
}

// Package runtime dependencies (SQLite) into the plugin jar so it can run standalone on the server.
tasks.named('jar') {
    from({
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    })
    duplicatesStrategy = DuplicatesStrategy.EXCLUDE
}
