# Build Anleitung

1) Java installieren (Java 17 empfohlen).
2) `./gradlew build` ausführen.
3) Die JAR liegt in `build/libs/`.
4) JAR nach `<hytale-server>/mods/` kopieren.
5) Server starten und `/zben ping` testen.
